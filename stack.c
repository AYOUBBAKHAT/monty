#include "monty.h"

/**
 * f_stack - Displays the element at the top of the stack
 * @head: Pointer to the top of the stack
 * @counter: Line number
 *
 * Return: No return value
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
